export enum Direction {
    UP = 'up',
    DOWN = 'down',
    LEFT = 'left',
    RIGHT = 'right'
}


export enum Rarity {
    NEVER = 'NEVER',
    COMMON = 'COMMON',
    RARE = 'RARE',
    EPIC = 'EPIC'
}
